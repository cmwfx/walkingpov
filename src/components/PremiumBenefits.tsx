import { Check, Download, Infinity as InfinityIcon, ShieldCheck } from 'lucide-react';
export function PremiumBenefits({ className = '' }: { className?: string }) { const benefits = [['Lifetime access', InfinityIcon], ['Protected premium downloads', Download], ['Private support for members', ShieldCheck]] as const; return <div className={`space-y-3 ${className}`}>{benefits.map(([label, Icon]) => <div className="flex items-center gap-3 text-sm text-slate-200" key={label}><span className="rounded-full bg-emerald-400/10 p-1.5 text-emerald-300"><Check className="h-3.5 w-3.5" /></span><Icon className="h-4 w-4 text-violet-300" />{label}</div>)}</div>; }
